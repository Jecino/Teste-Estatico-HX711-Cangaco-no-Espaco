# Como rodar o código

Primeiro inicie um venv:

> python3 -m venv .venv

Depois ative o venv:

> source .venv/bin/activate

Em seguida baixe os pacotes do arquivo de requerimentos:

> pip install -r requirements.txt

E agora só rodar o código:

> python3 serial_reader.py
